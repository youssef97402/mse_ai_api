version: "3.8"

services:
  mse-api:
    build: .
    container_name: mse_ai_api
    restart: unless-stopped
    ports:
      - "7777:7777"
    environment:
      - API_SECRET_KEY=change-secret-key-2026
      - PYTHONUNBUFFERED=1
